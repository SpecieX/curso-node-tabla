# Notes:
Primera practica Node

```
Opciones:
      --help     Muestra ayuda                                                  [booleano]
      --version  Muestra número de versión                                      [booleano]
  -b, --base     Establece el numero para crear la tabla de multiplicar         [número]    [requerido]
  -h, --hasta    Establece el numero final para crear la tabla de multiplicar   [número]    [defecto: "10"]
  -l, --listar   Establece si sera mostrada en pantalla la tabla creada         [booleano]  [defecto: false]
```